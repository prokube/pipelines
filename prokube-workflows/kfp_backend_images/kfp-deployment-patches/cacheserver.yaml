apiVersion: apps/v1
kind: Deployment
metadata:
  name: cache-server
spec:
  template:
    spec:
      containers:
      - name: server
        image: ${CI_IP_ADDRESS_AND_PORT}/kiss/prokube-images/cacheserver:latest
        imagePullPolicy: Always
      imagePullSecrets:
      - name: regcred
