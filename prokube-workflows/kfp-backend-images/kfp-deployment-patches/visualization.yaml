apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-pipeline-visualization-server
  spec:
  template:
    spec:
      containers:
      - name: ml-pipeline-visualization-server
        image: ${CI_IP_ADDRESS_AND_PORT}/kiss/prokube-images/visualization:latest
        imagePullPolicy: Always
      imagePullSecrets:
      - name: regcred
